import { Bounce, Zoom } from "react-toastify";

const toastConfig = {
  position: "top-right",
  autoClose: 1500,
  hideProgressBar: false,
  closeOnClick: false,
  pauseOnHover: true,
  draggable: true,
  theme: "dark",
  transition: Zoom,
};
export default toastConfig;
